<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Email Verification</title>
    <style>
      /* CSS Reset */
      body {
        font-family: Helvetica, sans-serif;
        -webkit-font-smoothing: antialiased;
        font-size: 16px;
        line-height: 1.3;
        -ms-text-size-adjust: 100%;
        -webkit-text-size-adjust: 100%;
      }

      html {
        margin: 0;
        padding: 0;
        font-family: Arial, sans-serif;
        line-height: 1.6;
      }

      /* Email Body */
      .container {
        max-width: 600px;
        margin: 0 auto;
        padding: 20px;
        border: 1px solid #ccc;
        border-radius: 15px;
      }

      .logo {
        text-align: center;
        margin-bottom: 20px;
      }

      .logo img {
        max-width: 200px;
      }

      .verification-text {
        text-align: center;
        margin-bottom: 20px;
      }

      .verification-token {
        text-align: center;
        font-size: 24px;
        font-weight: bold;
        margin-bottom: 20px;
      }

      .footer {
        text-align: center;
        margin-top: 20px;
      }
    </style>
  </head>
  <body>
    <div class="container">
      {{!-- <div class="logo">
        <img src="https://example.com/logo.png" alt="Company Logo" />
      </div> --}}
      <div class="verification-text">
        <p>Thank you for signing up!</p>
        <p>
          Please use the following verification token to complete your
          registration:
        </p>
      </div>
      <div class="verification-token">
        <p style="letter-spacing: 2px; background-color: darkgray; margin: 0 36%">{{token}}</p>
      </div>
      <div class="footer">
      <p>This token will expire in <b>3</b> minutes</p>
        <p>
          If you did not sign up for this account, please ignore this email.
        </p>
      </div>
    </div>
  </body>
</html>
